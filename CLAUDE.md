# 팡이케어 (PANG2CARE) 홈페이지

에어컨 청소 전문 업체 "팡이케어"의 예약형 홈페이지. 정적 HTML/CSS/JS로 작성되어 있고
별도 빌드 과정 없이 파일을 그대로 서빙한다. 백엔드는 구글 시트 + Apps Script 웹앱.

- GitHub 저장소: `pang2care/new-test`였다가 **`pang2care/PANG2CARE`로 이름 변경됨**
  (기존 `new-test` 주소로 push해도 GitHub가 자동 리다이렉트하므로 로컬 remote는 그대로 둬도 됨)
- 브랜치: `master`
- 사용자와의 대화는 항상 한국어로 (별도 메모리에도 저장되어 있음)

## 파일 구조

- `index.html` — 고객용 메인 홈페이지 (예약 폼, 갤러리, 후기, FAQ 등 전부 이 한 파일 안에 있음. 단일 파일이 커서 grep으로 섹션 찾는 걸 추천)
- `admin.html` — 관리자용 예약 캘린더 (비밀번호 로그인 후 예약 상태 변경/등록/삭제)
- `reservations-api.js` — `index.html`/`admin.html` 공용, 구글 시트 API 호출 함수(apiList/apiCreate/apiUpdate/apiDelete)와 TIME_SLOTS, STATUS_LABEL 등 상수
- `sheet-config.js` — 구글 Apps Script 웹앱 URL (`SHEET_API_URL`)
- `apps-script/Code.gs` — 구글 시트에 실제 배포되는 백엔드 소스. **로컬에서 이 파일을 고쳐도 자동 반영 안 됨** — 구글시트 → 확장 프로그램 → Apps Script 편집기에 직접 붙여넣고 "배포 → 배포관리 → 새 버전"으로 재배포해야 실제 반영됨. 관리자 비밀번호(ADMIN_KEY)도 이 파일 안에 있음
- `images/` — 홈페이지에서 실제 쓰는, 크롭·보정·EXIF 제거된 최종 이미지들 (gallery-01~06 + -2/-3/-4 변형, detail-before/process/after)
- `1/` — 에어컨 분해청소 원본 사진 76장 (사용자가 GitHub에 직접 업로드한 원본 raw 파일들, 용량 큼. 필요할 때 골라서 `images/`로 가공해서 쓰는 소스 풀)

## 디자인 톤

청록(`#0b7f89`, `#18b4bf`) + 코럴(`#ff6b4a`) 컬러 조합. 히어로 영역에 손수 그린 인라인 SVG
청소 마스코트 캐릭터(물방울 모양, 스프레이+걸레 들고 에어컨 청소하는 모습)가 있음 —
`.aircon-box` 안 `.aircon-svg`, viewBox `0 0 220 120` 크기 제약 있음 (박스 크기 키우지 말고
그 안에서 그림만 맞추는 걸 선호함 — 한 번 박스를 키웠다가 되돌린 적 있음).

## 지금까지 한 작업 (최근 세션)

1. **작업현황 갤러리**: PANG2CARE 원본 사진(`1/` 폴더)에서 대표 사진 골라 크롭/보정/EXIF 제거 후
   `images/`에 등록. 갤러리 카드 6개 각각 여러 장(4~5장)을 5초마다 자동 전환(페이드) 하도록 구현.
2. **히어로 대표 이미지**: 기존 추상적인 에어컨 일러스트를 청소 마스코트 캐릭터 SVG로 교체.
3. **고객 후기**: 당근비즈니스에서 실제로 받은 후기 73건을 사용자가 텍스트로 붙여넣어줘서
   `reviews` 배열(JS)에 반영. 카드 3개씩 5초마다 자동 순환. **가짜 후기는 절대 만들지 않음** —
   전부 실제 당근 후기 원문을 다듬은 것. 후기 사진은 아직 미반영(스크린샷 받으면 추가 예정).
4. **예약 시스템 실사용 테스트**: 예약 30건 + 취소 10건을 실제 구글시트에 만들어서 전체 플로우
   검증 후 전부 삭제(정리)함.
5. **버그 발견 및 수정**: 예약 시간이 `09:00`일 때 구글시트가 자동으로 시간값 인식해서 `9:00`으로
   앞자리 0이 사라지는 버그 발견 (→ 이중예약 가능해지는 문제였음). `Code.gs`에서 값 쓰기 전에
   셀 서식을 텍스트(`@`)로 먼저 지정하도록 수정, 사용자가 재배포 완료, 재검증까지 마침.

## 알아두면 좋은 것

- 이 코드스페이스 환경에서 `git fetch`/`pull`이 가끔 멈추는(hang) 문제가 있었음 — 그럴 땐
  `GIT_TERMINAL_PROMPT=0` + 짧은 timeout으로 재시도하거나, `git ls-remote`로 먼저 연결 확인.
- 사용자가 "깃 세이브"라고 하면 `git add` → `git commit` → `git push` 순서로 진행 (별도 메모리에 있음).
- 실제 서비스 중인 구글시트/Apps Script를 대상으로 테스트할 땐 이름에 `[테스트]` 등으로 표시하고
  끝나면 꼭 정리(삭제)할 것 — 실사용 데이터와 섞이면 안 됨.
- 이미지 처리는 Python Pillow(PIL) 사용 (`pip install Pillow` 되어있음), 브라우저 렌더링 확인은
  Playwright(`npx playwright install chromium`)로 스크린샷 찍어서 검증하는 방식을 씀.

## 다음에 할 일 (예정)

- 홈페이지 전반적인 꾸미기/디자인 다듬기
- 대표 이미지(히어로 이미지 등) 다시 정하기
- 후기 사진 반영 (스크린샷 받아서 매칭)
