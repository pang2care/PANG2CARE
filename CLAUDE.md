# 팡이케어 (PANG2CARE) 홈페이지

에어컨 청소 전문 업체 "팡이케어"의 예약형 홈페이지 + 관리자 페이지. 정적 HTML/CSS/JS로
작성되어 있고 별도 빌드 과정 없이 파일을 그대로 서빙한다. 백엔드는 구글 시트 + Apps Script 웹앱.

- GitHub 저장소: `pang2care/new-test`였다가 **`pang2care/PANG2CARE`로 이름 변경됨**
  (기존 `new-test` 주소로 push해도 GitHub가 자동 리다이렉트하므로 로컬 remote는 그대로 둬도 됨)
- 실제 배포 주소: **https://pang2care.github.io/PANG2CARE/** (GitHub Pages, push 후 반영까지 1~2분 소요)
- 브랜치: `master`
- 사용자와의 대화는 항상 한국어로 (별도 메모리에도 저장되어 있음)

## 파일 구조

- `index.html` — 고객용 메인 홈페이지 (예약 폼, 갤러리, 후기, FAQ 등 전부 이 한 파일 안에 있음. 단일 파일이 커서 grep으로 섹션 찾는 걸 추천)
- `admin.html` — 관리자 페이지. 사이드바 네비게이션으로 "예약 캘린더"(+ 예약 분석 통합됨) / AI 마케팅(대시보드·전후사진만들기·블로그작성·홍보콘텐츠·리뷰답변) / AI 설정 을 오가는 구조
- `reservations-api.js` — `index.html`/`admin.html` 공용, 구글 시트 API 호출 함수(apiList/apiCreate/apiUpdate/apiDelete)와 TIME_SLOTS, STATUS_LABEL 등 상수
- `sheet-config.js` — 구글 Apps Script 웹앱 URL (`SHEET_API_URL`)
- `apps-script/Code.gs` — 구글 시트에 실제 배포되는 백엔드 소스. **로컬에서 이 파일을 고쳐도 자동 반영 안 됨** — 구글시트 → 확장 프로그램 → Apps Script 편집기에 직접 붙여넣고 "배포 → 배포관리 → 새 버전"으로 재배포해야 실제 반영됨. 관리자 비밀번호(ADMIN_KEY = `1354`)도 이 파일 안에 있음
- `images/` — 홈페이지에서 실제 쓰는 최종 이미지들 (gallery-01~06 + -2/-3/-4 변형, detail-before/process/after, hero-before-after.jpg)
- `1/` — 에어컨 분해청소 원본 사진 76장 (사용자가 GitHub에 직접 업로드한 원본 raw 파일들, 용량 큼. 필요할 때 골라서 `images/`로 가공해서 쓰는 소스 풀)
- `업로드/` — **사용자가 파일을 던져두는 전용 드롭 폴더** (`.gitignore` 처리되어 커밋 안 됨). 사용자가 "업로드 폴더 확인해봐" 라고 하면 이 폴더 안을 바로 확인할 것 — 매번 VS Code 드래그 방법을 재설명할 필요 없음

## 디자인 톤

청록(`#0b7f89`, `#18b4bf`) + 코럴(`#ff6b4a`) 컬러 조합.

히어로 대표 이미지(`.aircon-box` 안 `.aircon`)는 손수 그린 SVG 마스코트 → 이후 사용자가
Gemini로 생성한 홍보 콜라주에서 "비교가 안되는 깨끗함!" 청소 전/후 비교 패널만 크롭한
`images/hero-before-after.jpg`로 최종 교체됨. `.aircon-photo` 클래스로 object-fit:cover 처리.

**사용자는 레이아웃/박스 크기를 잘 안 건드리길 선호함** — 큰 구조 변경보다 그 틀 안에서
내용만 바꾸는 걸 선호. (예: 대표 이미지 SVG→마스코트 교체 시 박스를 키웠다가 "그대로 두고
이미지만 바꾸라"는 피드백을 받고 원래 크기로 되돌린 적 있음.)

## 지금까지 한 작업 (요약, 최신순 아님 — 카테고리별)

**고객 홈페이지 (index.html)**
- 작업현황 갤러리: 카드 6개, 각각 사진 4~5장을 5초마다 자동 전환(페이드)
- 고객 후기: 당근비즈니스 실제 후기 **73건**을 `reviews` 배열에 반영, 카드 3개씩 5초마다 자동 순환.
  **가짜 후기는 절대 만들지 않음** — 전부 실제 당근 후기 원문을 다듬은 것
- 히어로 대표 이미지: `images/hero-before-after.jpg` (위 디자인 톤 항목 참고)
- 비주얼 리팩터: 사용자가 준 CSS를 기존 콘텐츠 위에 얹는 방식으로 적용 (히어로 그라데이션,
  카드 그림자, 버튼 반짝임 등). 이 과정에서 모바일 680px 이하 메뉴가 `display:none`을 물려받아
  안 보이던 버그 발견·수정함
- 서비스 카드 4개 하단 태그가 설명 문구 길이에 따라 줄이 안 맞던 버그 수정 (`.service-card`를
  flex column + `margin-top:auto`로 정렬)
- 09:00 시간대 이중예약 가능 버그 발견·수정 (아래 참고)

**관리자 페이지 (admin.html) — 대대적으로 재구성함**
- 기존 단일 캘린더 화면 → 사이드바 네비게이션 구조로 전면 개편
- "예약 캘린더"와 "예약 분석"을 하나로 통합: 캘린더에 예약 없는 미래 날짜를
  코럴색+"홍보추천" 태그로 표시, 하단에 AI 공백 분석 텍스트 표시
- AI 마케팅 섹션 추가: AI 대시보드(실제 예약 데이터 기반 지표), AI 블로그 작성(사진 첨부 가능),
  홍보 콘텐츠, 리뷰 답변
- **중요: 이 "AI" 생성 기능들은 실제 AI 호출이 아니라 입력값을 문장 템플릿에 끼워 맞추는
  방식임** — 사용자가 명시적으로 "템플릿 방식 그대로" 선택함, 비용 없음, 즉시 응답
- 사진 첨부 기능(블로그)은 **브라우저 미리보기만 제공, 서버 업로드 없음** (백엔드에
  이미지 저장소가 없어서). 실제 게시할 땐 사용자가 직접 첨부해야 함
- AI 콘텐츠 생성 이력/설정은 `localStorage`에 저장 (기기별로 다름, 서버 동기화 안 됨)
- **3채널 발행 자동화** (AI 설정 → "블로그 작성 시 3채널 발행 목록"을 "사용"으로 켜면 작동):
  AI 블로그 작성에서 초안을 만들면 네이버 블로그·네이버 스마트플레이스·당근마켓 소식 3개
  초안이 한 번에 생성되어 "3채널 발행 목록"에 카드로 쌓임. 각 카드는 그 자리에서 수정
  가능하고, "승인"을 누르면 내용이 클립보드에 복사되고 AI 설정에 등록해둔 사이트 주소가
  새 창으로 열림 (마지막 붙여넣기·실제 게시는 사람이 직접). 이 반자동 방식을 택한 이유:
  네이버 블로그는 공식 오픈API로 실제 자동 게시가 가능하지만(추후 OAuth 연동 필요, 미구현),
  네이버 스마트플레이스·당근마켓은 글쓰기 공식 API가 없어 자동 로그인/게시를 하려면
  브라우저 자동화가 필요한데 이는 이용약관 위반·계정정지 위험이 있어 채택하지 않음.
  이 기능이 생기면서 기존에 별도로 있던 "당근 소식 작성" 사이드바 메뉴는 중복이라 삭제함
  (해당 소식글도 이제 AI 블로그 작성 → 3채널 발행 목록에서 함께 나옴). 발행 목록도
  `localStorage`에 저장 (기기별로 다름, 서버 동기화 안 됨)

**백엔드 (apps-script/Code.gs)**
- 버그 수정: `09:00` 같은 시간이 구글시트에 저장될 때 시간으로 자동 인식되어 `9:00`으로
  앞자리 0이 사라지는 문제 (→ 이중예약 가능해지는 심각한 버그였음). 값을 쓰기 전에 셀
  서식을 텍스트(`@`)로 먼저 지정하도록 `handleCreate_`/`handleUpdate_` 수정, 재배포·재검증 완료

**테스트**
- 예약 생성/수정/삭제/상태전이/취소요청·승인·거절/인증 실패 케이스 등 백엔드 41개 항목,
  실제 UI로 예약 신청→조회→취소요청 9개 항목, 이중예약 방지(프론트 슬롯 비활성화) 등
  실제 시스템 대고 전부 검증 완료 (테스트 데이터는 `[테스트]` 표시 후 전부 정리함)
- **구글 Apps Script 자체가 요청당 2.4~3.2초 정도 걸림** (확인된 사실, 버그 아니고 무료
  백엔드 특성). 관리자 페이지에서 버튼 연타하면 이전 요청이 안 끝난 상태에서 다음 요청이
  나가 상태가 꼬일 수 있음 — 테스트 스크립트 짤 때 상태 배지 텍스트가 실제로 바뀔 때까지
  `waitForFunction`으로 기다리고, 최소 15~20초 타임아웃을 줄 것

## 알아두면 좋은 것

- 이 코드스페이스 환경에서 `git fetch`/`pull`이 가끔 멈추는(hang) 문제가 있었음 — 그럴 땐
  `GIT_TERMINAL_PROMPT=0` + 짧은 timeout으로 재시도하거나, `git ls-remote`로 먼저 연결 확인.
- 사용자가 "깃 세이브"라고 하면 `git add` → `git commit` → `git push` 순서로 진행 (별도 메모리에 있음).
- 실제 서비스 중인 구글시트/Apps Script를 대상으로 테스트할 땐 이름에 `[테스트]` 등으로 표시하고
  끝나면 꼭 정리(삭제)할 것 — 실사용 데이터와 섞이면 안 됨.
- 이미지 처리는 Python Pillow(PIL) 사용, 브라우저 렌더링 확인은 Playwright(스크린샷)로 검증.
  로컬 테스트 서버(`python3 -m http.server`)를 백그라운드로 띄울 때 이 환경에서 가끔
  프로세스가 조기 종료되는 경우가 있었음 — `nohup ... & disown` 후 `curl`로 살아있는지
  먼저 확인하고 나서 Playwright를 실행하면 안정적임. 여러 단계를 한 Bash 호출에 몰아넣기보다
  단계별로 나눠 호출하는 게 이 환경에서 더 안정적이었음.
- Gemini 채팅(gemini.google.com/app/...) 같은 로그인 필요한 링크는 못 봄 — 사용자가 파일로
  내려받아서 `업로드/` 폴더에 넣어줘야 확인 가능.

## 다음에 할 일 (사용자가 아직 명시적으로 요청 안 함, 참고용)

- 후기 사진 반영 (스크린샷 받아서 어떤 후기인지 매칭 후 추가) — 이전에 시도했으나 텍스트
  복사로는 사진이 안 넘어와서 보류된 상태
- 필요하면 admin.html의 "AI" 생성 기능을 실제 AI(Claude API 등) 연동으로 업그레이드 가능
  (Apps Script에서 UrlFetchApp으로 외부 API 호출하는 방식 검토 가능, 비용 발생함)
- 네이버 블로그 실제 자동 게시 (3채널 발행 목록의 다음 단계): 네이버 개발자센터에 앱을
  등록하고 OAuth 로그인 연동을 해야 하는데, 이건 사용자가 직접 네이버 계정으로 앱을
  등록하는 외부 작업부터 시작해야 함 — 요청받으면 등록 절차 안내 후 Apps Script에
  콜백 처리·토큰 저장·블로그 글쓰기 API 호출 로직 추가하면 됨
