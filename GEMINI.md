# 팡이케어 (PANG2CARE) 홈페이지

에어컨 청소 전문 업체 "팡이케어"의 예약형 홈페이지 + 관리자 페이지. 정적 HTML/CSS/JS로
작성되어 있고 별도 빌드 과정 없이 파일을 그대로 서빙한다. 백엔드는 구글 시트 + Apps Script 웹앱.

> 이 파일은 클로드 코드가 쓰는 `CLAUDE.md`와 같은 프로젝트를 제미나이(Gemini CLI)용으로
> 정리한 것. 기술적인 내용은 `CLAUDE.md`와 동일하게 유지하려고 하니, 둘 중 하나만 고치고
> 다른 하나를 깜빡했다면 서로 비교해서 맞춰줄 것.

- GitHub 저장소: `pang2care/new-test`였다가 **`pang2care/PANG2CARE`로 이름 변경됨**
  (기존 `new-test` 주소로 push해도 GitHub가 자동 리다이렉트하므로 로컬 remote는 그대로 둬도 됨)
- 실제 배포 주소: **https://pang2care.github.io/PANG2CARE/** (GitHub Pages, push 후 반영까지 1~2분 소요)
- 브랜치: `master`
- 사용자와의 대화는 항상 한국어로

## 대화 방식 (중요 — 이대로 유지할 것)

- 사용자는 짧고 캐주얼한 말투(반말 섞인 채팅체)로 요청함. 정중하지만 간결한 해요체로
  답할 것 — 길게 설명하지 말고 핵심만.
- 사용자는 스크린샷을 자주 첨부함. 화면 속 정확한 버튼/메뉴 라벨을 그대로 인용해서
  안내할 것(예: "배포 관리 → 연필 아이콘 → 새 버전") — "그 버튼 눌러주세요" 식으로
  뭉뚱그리지 말 것.
- 정확히 요청받은 범위만 작업. 범위 밖 수정이 필요해 보이면 먼저 물어보고 진행.
- 문제가 안 풀릴 때는 사용자에게 반복해서 시키기보다, 가능하면 먼저 직접
  재현·테스트해서 가설을 좁힌 다음, 꼭 필요한 최소한의 조작만 요청할 것.
- 다음 확인/조치 단계는 번호 매긴 목록("1. ~ 2. ~ 3. ~")으로 제시하는 걸 선호함.

## 앞으로 계속 적용할 스킬

레이아웃/CSS 작업 시 `responsive-design`(모바일 우선), 색상/폰트 새로 고를 때 `theme-factory`
(기존 청록/코럴 토큰 재사용 우선), 고객용 폼/버튼 만질 때 `accessibility-check`(대비·시맨틱
태그·alt 텍스트), 코드 고친 뒤 `webapp-testing`(스크린샷/클릭 실제 검증)을 기본으로 반영.
단, 이것 때문에 먼저 나서서 기존 코드를 감사(audit)하거나 리팩터링하지는 않음 — 명시적
점검 요청 시에만 전체 감사, 그 외엔 다른 작업 중 우연히 발견하면 알리고 확인 후 고치는 정도.

## 파일 구조

- `index.html` — 고객용 메인 홈페이지 (예약 폼, 갤러리, 후기, FAQ 등 전부 이 한 파일 안에 있음. 단일 파일이 커서 grep으로 섹션 찾는 걸 추천)
- `admin.html` — 관리자 페이지. 사이드바 네비게이션으로 "예약 캘린더"(+ 예약 분석 통합됨) / AI 마케팅(대시보드·전후사진만들기·블로그작성·홍보콘텐츠·홍보 영상 만들기·리뷰답변) / AI 설정 을 오가는 구조
- `reservations-api.js` — `index.html`/`admin.html` 공용, 구글 시트 API 호출 함수(apiList/apiCreate/apiUpdate/apiDelete)와 TIME_SLOTS, STATUS_LABEL 등 상수
- `sheet-config.js` — 구글 Apps Script 웹앱 URL (`SHEET_API_URL`)
- `apps-script/Code.gs` — 구글 시트에 실제 배포되는 백엔드 소스. **로컬에서 이 파일을 고쳐도 자동 반영 안 됨** — 구글시트 → 확장 프로그램 → Apps Script 편집기에 직접 붙여넣고 "배포 → 배포관리 → 새 버전"으로 재배포해야 실제 반영됨. 관리자 비밀번호(ADMIN_KEY = `1354`)도 이 파일 안에 있음
- `images/` — 홈페이지에서 실제 쓰는 최종 이미지들
- `music/` — 관리자 페이지 "홍보 영상 만들기"에서 기본 제공하는 배경음악 파일들 (저작권 확인된 파일만 추가할 것)
- `1/` — 에어컨 분해청소 원본 사진 76장 (원본 raw 파일들, 용량 큼. 필요할 때 골라서 `images/`로 가공해서 쓰는 소스 풀)
- `업로드/` — **사용자가 파일을 던져두는 전용 드롭 폴더** (`.gitignore` 처리되어 커밋 안 됨). "업로드 폴더 확인해봐"라고 하면 이 폴더 안을 바로 확인할 것

## 디자인 톤

청록(`#0b7f89`, `#18b4bf`) + 코럴(`#ff6b4a`) 컬러 조합.

히어로 대표 이미지(`.aircon-box` 안 `.aircon`)는 손수 그린 SVG 마스코트 → 이후
`images/hero-before-after.jpg`(Gemini로 생성한 홍보 콜라주에서 전/후 비교 패널만 크롭)로
최종 교체됨. `.aircon-photo` 클래스로 object-fit:cover 처리.

## 백엔드 참고 (apps-script/Code.gs)

- 카카오톡 새 예약 알림: `handleCreate_`에서 카카오 "나에게 보내기" API로 사장님 본인
  카톡에 알림 전송. `KAKAO_REST_API_KEY`는 하드코딩 가능(공개돼도 되는 키), 클라이언트
  시크릿은 절대 넣지 않음. Redirect URI는 "앱 > 플랫폼 키 > REST API 키 > 카카오 로그인
  리다이렉트 URI"에 등록. "카카오톡 메시지 전송" 동의항목은 "선택 동의"로 둬야 정상 동작
  (필수 동의로 하면 `insufficient scopes` 403 에러). 완전 무료, 단 소리/푸시 알림은 안 옴
  (나에게 보내기 API 특성).
- `09:00` 같은 시간이 구글시트에서 `9:00`으로 앞자리 0이 사라져 이중예약 가능해지던 버그
  수정됨 — 값 쓰기 전 셀 서식을 텍스트(`@`)로 지정.

## 이 프로젝트에서 사용자와 맞춰온 개인화된 작업 습관

클로드 코드와 여러 세션에 걸쳐 맞춰온 습관들. 처음부터 다시 설명 안 해도 되도록 여기 정리함.

- **항상 한국어로 대답할 것.** 사용자가 영어로 물어도 한국어로 답한다.
- **"깃 세이브"** = `git add .` → `git commit`(저장소 기존 커밋 메시지 스타일에 맞게 작성) → `git push` 를 순서대로, 단계별 확인 없이 한 번에 실행하라는 뜻. 단, `git status`/`git diff`로 커밋 전 내용 확인하고 민감정보 없는지 체크하는 등 기본 안전 수칙은 그대로 지킬 것.
- **코드 수정 전에 미리 승인받으려 하지 말 것.** 진행 방식이나 계획을 먼저 물어보지 말고 바로 수정한 다음, 수정한 내용을 보고하고 사용자가 사후에 확인하는 방식을 선호함. (단, force-push나 `git reset --hard`처럼 되돌리기 어려운 진짜 위험한 작업은 여전히 사전 확인 필요.)
- **레이아웃/박스 크기를 잘 안 건드리길 선호함.** 큰 구조 변경보다는 기존 틀 안에서 내용만 바꾸는 걸 선호.
- **완성된 배너/그래픽 이미지를 참고자료로 주면, 그 이미지를 그대로(wholesale) 써야 함.** 직접 CSS/SVG로 재현하거나, 이미지를 잘라서 재조합하는 식으로 "똑똑하게" 처리하려 하지 말 것 — 두 번이나 그렇게 했다가 거절당한 적 있음("올려놓은 이미지 그대로 사용이 안돼?"). 여백만 잘라내고 하나의 이미지 에셋으로 그대로 삽입하는 게 기본값.
- **팡이케어 프로젝트의 3대 구성요소를 다음 줄임말로 부름** — 설명 없이 바로 알아들어야 함:
  - **홈1** = 고객용 홈페이지 (`index.html`)
  - **구1** = 구글 시트/Apps Script 백엔드 (`apps-script/Code.gs`, 배포된 웹앱, 시트 자체)
  - **예1** = 관리자 예약 캘린더 페이지 (`admin.html`)
- **`apps-script/Code.gs` 코드는 항상 파일에 직접 반영할 것, 채팅에 코드블록으로 붙여넣지 말 것.** 사용자가 이 코드를 구글 Apps Script 편집기에 복사해 넣어야 할 때, 채팅창 코드블록은 복사 아이콘이 안 뜨거나 복사 시 내용이 깨지는 문제가 있었음. 대신 GitHub 파일 링크(`https://github.com/<owner>/<repo>/blob/<branch>/<path>`)를 줄 것 — GitHub 파일 뷰어의 복사 아이콘은 정상 작동함. 사용자가 명시적으로 채팅에 보여달라고 할 때만 코드블록으로 보여줄 것.
- **네이버 블로그(https://blog.naver.com/pang2care) 디자인/스킨 작업 시 기존 게시글을 절대 삭제/손실시키지 말 것.** 스킨/테마 변경이 위젯 설정이나 레이아웃을 리셋할 수 있으니, 디자인/스킨/위젯 설정만 건드리고 게시글 내용에는 영향 없는 방식으로 안내할 것. 게시글에 영향 줄 수 있는 단계가 있다면 진행 전에 반드시 먼저 알릴 것.
- 실제 서비스 중인 구글시트/Apps Script를 대상으로 테스트할 땐 이름에 `[테스트]` 등으로 표시하고 끝나면 꼭 정리(삭제)할 것 — 실사용 데이터와 섞이면 안 됨.
- 구글 Apps Script 자체가 요청당 2.4~3.2초 정도 걸림 (버그 아니고 무료 백엔드 특성). 관리자 페이지에서 버튼 연타하면 이전 요청이 안 끝난 상태에서 다음 요청이 나가 상태가 꼬일 수 있음.

더 자세한 프로젝트 진행 이력(지금까지 한 작업, 다음에 할 일 등)은 저장소 루트의 `CLAUDE.md`를 참고.
